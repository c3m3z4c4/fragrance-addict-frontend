# URL del backend API
# En Dokploy: pon la URL que te asignó Dokploy al backend, ej:
#   https://api.tudominio.com
# Esta variable se inyecta en el BUILD (no en runtime), así que
# hay que configurarla como Build Arg en Dokploy con nombre VITE_API_URL
VITE_API_URL=https://api.tudominio.com
